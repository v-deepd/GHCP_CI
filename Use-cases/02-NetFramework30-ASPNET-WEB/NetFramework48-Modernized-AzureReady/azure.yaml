# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: netframework30-aspnetweb
metadata:
  template: netframework30-aspnetweb@0.0.1-beta

services:
  web:
    project: .
    language: dotnet
    host: appservice
    
infra:
  provider: terraform
  path: infra
  
